# i18n API

O V12 tem suporte nativo a internacionalização com:

- configuração global em `createApp()`
- traduções locais por módulo
- helper `t()` no `RequestContext`
- `I18nService`

## Configuração global

```ts
const app = await createApp({
  i18n: {
    defaultLocale: 'pt',
    localesPath: './locales',
  },
});
```

## `I18nOptions`

```ts
type I18nOptions = {
  defaultLocale: string;
  localesPath?: string;
}
```

## Arquivos JSON

Se `localesPath` for informado, o serviço carrega arquivos `.json` do diretório.

Exemplo:

`./locales/en.json`

```json
{
  "welcome": "Welcome, {{name}}!"
}
```

`./locales/pt.json`

```json
{
  "welcome": "Bem-vindo, {{name}}!"
}
```

## Traduções por módulo

Cada módulo também pode declarar `i18n`.

```ts
export const BillingModule = defineModule({
  name: 'billing',
  i18n: {
    en: {
      billing: {
        paid: 'Invoice paid',
      },
    },
    pt: {
      billing: {
        paid: 'Fatura paga',
      },
    },
  },
});
```

Essas traduções são mescladas no bootstrap.

## Uso no handler

`RequestContext` já expõe `t(key, args?)`.

```ts
router.get('/greet', {
  handler: ({ t, request }) => {
    const name = (request.query as any).name || 'Guest';
    return {
      message: t('welcome', { name }),
    };
  },
});
```

## Como o locale é resolvido

Na implementação atual do contexto:

1. `request.query.lang`
2. `accept-language` simplificado para o prefixo antes de `-`
3. `defaultLocale`

Então:

- `?lang=pt`
- header `accept-language: pt-BR`

ambos podem resolver para `pt`.

## `I18nService`

Também pode ser resolvido diretamente pelo token:

```txt
'I18nService'
```

## Métodos úteis

### `translate(key, locale?, args?)`

```ts
const text = i18n.translate('welcome', 'pt', { name: 'João' });
```

### `addTranslations(locale, data)`

```ts
i18n.addTranslations('en', {
  hello: 'Hello',
});
```

### `loadTranslationsFromPath(path)`

```ts
i18n.loadTranslationsFromPath('./locales');
```

## Interpolação

O formato é:

```txt
{{name}}
```

Exemplo:

```json
{
  "welcome": "Bem-vindo, {{name}}!"
}
```

## Limites atuais

- o contexto usa `lang` em query; não lê `locale` por query no código atual
- a detecção por header reduz `pt-BR` para `pt`
- o `I18nService` trabalha com objetos JSON simples e lookup por chave aninhada

## Links relacionados

- [createApp](/api/create-app)
- [Modules](/concepts/modules)
